[package]
name = "go_zkvm_host"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
base64 = "0.22"
host-core = { path = "../host-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
